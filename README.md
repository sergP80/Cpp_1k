# C++/CMake demo projects

## Build and test

* Create new directory and go to it
   ```
    mkdir -p build && cd build
   ```
* Run CMake
  ```
  cmake ..
  ```
* Build project
  ```
  cmake --build .
  ```
* Run tests
  ```
  ctest
  ```